using System.Collections.Generic;
using System.Threading.Tasks;
using TestProject.Data.Entities;

namespace TestProject.Repositories
{
    public interface ICarRepository
    {
        // GetByGarage(garageId);

        // Get(carId);

        // Create(car);

        // Update(car, *);

        // Delete(carEntity *);
    }
}